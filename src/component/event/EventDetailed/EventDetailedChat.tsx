import * as React from 'react';

interface EventDetailedChatProps {
}

const EventDetailedChat: React.SFC<EventDetailedChatProps> = (props) => {
  return (
    <div>
      event datial EventDetailedChat
    </div>
  )
};

export default EventDetailedChat;