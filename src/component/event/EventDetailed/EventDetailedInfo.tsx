import * as React from 'react';

interface EventDetailedInfoProps {
}

const EventDetailedInfo: React.SFC<EventDetailedInfoProps> = (props) => {
  return (
    <div>
      event datial EventDetailedInfo
    </div>
  )
};

export default EventDetailedInfo;